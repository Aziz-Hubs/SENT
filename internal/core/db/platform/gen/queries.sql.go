// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.25.0
// source: queries.sql

package platform

import (
	"context"
)

const createTenant = `-- name: CreateTenant :one
INSERT INTO tenants (
  org_id, db_name, name, domain, tier, auth_type, region, status
) VALUES (
  $1, $2, $3, $4, $5, $6, $7, $8
)
RETURNING org_id, db_name, name, domain, tier, auth_type, region, status, created_at
`

type CreateTenantParams struct {
	OrgID    string
	DbName   string
	Name     string
	Domain   string
	Tier     string
	AuthType string
	Region   string
	Status   string
}

func (q *Queries) CreateTenant(ctx context.Context, arg CreateTenantParams) (Tenant, error) {
	row := q.db.QueryRow(ctx, createTenant,
		arg.OrgID,
		arg.DbName,
		arg.Name,
		arg.Domain,
		arg.Tier,
		arg.AuthType,
		arg.Region,
		arg.Status,
	)
	var i Tenant
	err := row.Scan(
		&i.OrgID,
		&i.DbName,
		&i.Name,
		&i.Domain,
		&i.Tier,
		&i.AuthType,
		&i.Region,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const getTenantByDBName = `-- name: GetTenantByDBName :one
SELECT org_id, db_name, name, domain, tier, auth_type, region, status, created_at FROM tenants
WHERE db_name = $1 LIMIT 1
`

func (q *Queries) GetTenantByDBName(ctx context.Context, dbName string) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantByDBName, dbName)
	var i Tenant
	err := row.Scan(
		&i.OrgID,
		&i.DbName,
		&i.Name,
		&i.Domain,
		&i.Tier,
		&i.AuthType,
		&i.Region,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const getTenantByOrgID = `-- name: GetTenantByOrgID :one
SELECT org_id, db_name, name, domain, tier, auth_type, region, status, created_at FROM tenants
WHERE org_id = $1 LIMIT 1
`

func (q *Queries) GetTenantByOrgID(ctx context.Context, orgID string) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantByOrgID, orgID)
	var i Tenant
	err := row.Scan(
		&i.OrgID,
		&i.DbName,
		&i.Name,
		&i.Domain,
		&i.Tier,
		&i.AuthType,
		&i.Region,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const listTenants = `-- name: ListTenants :many
SELECT org_id, db_name, name, domain, tier, auth_type, region, status, created_at FROM tenants
ORDER BY created_at DESC
`

func (q *Queries) ListTenants(ctx context.Context) ([]Tenant, error) {
	rows, err := q.db.Query(ctx, listTenants)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Tenant
	for rows.Next() {
		var i Tenant
		if err := rows.Scan(
			&i.OrgID,
			&i.DbName,
			&i.Name,
			&i.Domain,
			&i.Tier,
			&i.AuthType,
			&i.Region,
			&i.Status,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTenantStatus = `-- name: UpdateTenantStatus :exec
UPDATE tenants SET status = $1 WHERE org_id = $2
`

type UpdateTenantStatusParams struct {
	Status string
	OrgID  string
}

func (q *Queries) UpdateTenantStatus(ctx context.Context, arg UpdateTenantStatusParams) error {
	_, err := q.db.Exec(ctx, updateTenantStatus, arg.Status, arg.OrgID)
	return err
}

const deleteTenant = `-- name: DeleteTenant :exec
DELETE FROM tenants WHERE org_id = $1
`

func (q *Queries) DeleteTenant(ctx context.Context, orgID string) error {
	_, err := q.db.Exec(ctx, deleteTenant, orgID)
	return err
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"sent/ent/budgetforecast"
	"sent/ent/tenant"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// BudgetForecast is the model entity for the BudgetForecast schema.
type BudgetForecast struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Year holds the value of the "year" field.
	Year int `json:"year,omitempty"`
	// Month holds the value of the "month" field.
	Month int `json:"month,omitempty"`
	// ProjectedAmount holds the value of the "projected_amount" field.
	ProjectedAmount float64 `json:"projected_amount,omitempty"`
	// ActualSpent holds the value of the "actual_spent" field.
	ActualSpent float64 `json:"actual_spent,omitempty"`
	// ForecastData holds the value of the "forecast_data" field.
	ForecastData map[string]interface{} `json:"forecast_data,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BudgetForecastQuery when eager-loading is set.
	Edges                   BudgetForecastEdges `json:"edges"`
	tenant_budget_forecasts *int
	selectValues            sql.SelectValues
}

// BudgetForecastEdges holds the relations/edges for other nodes in the graph.
type BudgetForecastEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BudgetForecastEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BudgetForecast) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case budgetforecast.FieldForecastData:
			values[i] = new([]byte)
		case budgetforecast.FieldProjectedAmount, budgetforecast.FieldActualSpent:
			values[i] = new(sql.NullFloat64)
		case budgetforecast.FieldID, budgetforecast.FieldYear, budgetforecast.FieldMonth:
			values[i] = new(sql.NullInt64)
		case budgetforecast.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case budgetforecast.ForeignKeys[0]: // tenant_budget_forecasts
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BudgetForecast fields.
func (_m *BudgetForecast) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case budgetforecast.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case budgetforecast.FieldYear:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field year", values[i])
			} else if value.Valid {
				_m.Year = int(value.Int64)
			}
		case budgetforecast.FieldMonth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field month", values[i])
			} else if value.Valid {
				_m.Month = int(value.Int64)
			}
		case budgetforecast.FieldProjectedAmount:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field projected_amount", values[i])
			} else if value.Valid {
				_m.ProjectedAmount = value.Float64
			}
		case budgetforecast.FieldActualSpent:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field actual_spent", values[i])
			} else if value.Valid {
				_m.ActualSpent = value.Float64
			}
		case budgetforecast.FieldForecastData:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field forecast_data", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.ForecastData); err != nil {
					return fmt.Errorf("unmarshal field forecast_data: %w", err)
				}
			}
		case budgetforecast.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case budgetforecast.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field tenant_budget_forecasts", value)
			} else if value.Valid {
				_m.tenant_budget_forecasts = new(int)
				*_m.tenant_budget_forecasts = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BudgetForecast.
// This includes values selected through modifiers, order, etc.
func (_m *BudgetForecast) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the BudgetForecast entity.
func (_m *BudgetForecast) QueryTenant() *TenantQuery {
	return NewBudgetForecastClient(_m.config).QueryTenant(_m)
}

// Update returns a builder for updating this BudgetForecast.
// Note that you need to call BudgetForecast.Unwrap() before calling this method if this BudgetForecast
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BudgetForecast) Update() *BudgetForecastUpdateOne {
	return NewBudgetForecastClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BudgetForecast entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BudgetForecast) Unwrap() *BudgetForecast {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BudgetForecast is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BudgetForecast) String() string {
	var builder strings.Builder
	builder.WriteString("BudgetForecast(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("year=")
	builder.WriteString(fmt.Sprintf("%v", _m.Year))
	builder.WriteString(", ")
	builder.WriteString("month=")
	builder.WriteString(fmt.Sprintf("%v", _m.Month))
	builder.WriteString(", ")
	builder.WriteString("projected_amount=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProjectedAmount))
	builder.WriteString(", ")
	builder.WriteString("actual_spent=")
	builder.WriteString(fmt.Sprintf("%v", _m.ActualSpent))
	builder.WriteString(", ")
	builder.WriteString("forecast_data=")
	builder.WriteString(fmt.Sprintf("%v", _m.ForecastData))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BudgetForecasts is a parsable slice of BudgetForecast.
type BudgetForecasts []*BudgetForecast

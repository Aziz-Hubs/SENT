// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"sent/ent/user"
	"sent/ent/vaultfavorite"
	"sent/ent/vaultitem"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// VaultFavorite is the model entity for the VaultFavorite schema.
type VaultFavorite struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VaultFavoriteQuery when eager-loading is set.
	Edges                   VaultFavoriteEdges `json:"edges"`
	user_favorites          *int
	vault_item_favorited_by *int
	selectValues            sql.SelectValues
}

// VaultFavoriteEdges holds the relations/edges for other nodes in the graph.
type VaultFavoriteEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Item holds the value of the item edge.
	Item *VaultItem `json:"item,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VaultFavoriteEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// ItemOrErr returns the Item value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VaultFavoriteEdges) ItemOrErr() (*VaultItem, error) {
	if e.Item != nil {
		return e.Item, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: vaultitem.Label}
	}
	return nil, &NotLoadedError{edge: "item"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VaultFavorite) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case vaultfavorite.FieldID:
			values[i] = new(sql.NullInt64)
		case vaultfavorite.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case vaultfavorite.ForeignKeys[0]: // user_favorites
			values[i] = new(sql.NullInt64)
		case vaultfavorite.ForeignKeys[1]: // vault_item_favorited_by
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VaultFavorite fields.
func (_m *VaultFavorite) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case vaultfavorite.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case vaultfavorite.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case vaultfavorite.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_favorites", value)
			} else if value.Valid {
				_m.user_favorites = new(int)
				*_m.user_favorites = int(value.Int64)
			}
		case vaultfavorite.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field vault_item_favorited_by", value)
			} else if value.Valid {
				_m.vault_item_favorited_by = new(int)
				*_m.vault_item_favorited_by = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VaultFavorite.
// This includes values selected through modifiers, order, etc.
func (_m *VaultFavorite) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the VaultFavorite entity.
func (_m *VaultFavorite) QueryUser() *UserQuery {
	return NewVaultFavoriteClient(_m.config).QueryUser(_m)
}

// QueryItem queries the "item" edge of the VaultFavorite entity.
func (_m *VaultFavorite) QueryItem() *VaultItemQuery {
	return NewVaultFavoriteClient(_m.config).QueryItem(_m)
}

// Update returns a builder for updating this VaultFavorite.
// Note that you need to call VaultFavorite.Unwrap() before calling this method if this VaultFavorite
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *VaultFavorite) Update() *VaultFavoriteUpdateOne {
	return NewVaultFavoriteClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the VaultFavorite entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *VaultFavorite) Unwrap() *VaultFavorite {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: VaultFavorite is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *VaultFavorite) String() string {
	var builder strings.Builder
	builder.WriteString("VaultFavorite(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// VaultFavorites is a parsable slice of VaultFavorite.
type VaultFavorites []*VaultFavorite

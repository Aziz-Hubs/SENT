// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"sent/ent/tenant"
	"sent/ent/user"
	"sent/ent/voicemail"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Voicemail is the model entity for the Voicemail schema.
type Voicemail struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Caller holds the value of the "caller" field.
	Caller string `json:"caller,omitempty"`
	// AudioPath holds the value of the "audio_path" field.
	AudioPath string `json:"audio_path,omitempty"`
	// Transcription holds the value of the "transcription" field.
	Transcription string `json:"transcription,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Duration holds the value of the "duration" field.
	Duration int `json:"duration,omitempty"`
	// ReadAt holds the value of the "read_at" field.
	ReadAt time.Time `json:"read_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VoicemailQuery when eager-loading is set.
	Edges             VoicemailEdges `json:"edges"`
	tenant_voicemails *int
	user_voicemails   *int
	selectValues      sql.SelectValues
}

// VoicemailEdges holds the relations/edges for other nodes in the graph.
type VoicemailEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VoicemailEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VoicemailEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Voicemail) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case voicemail.FieldID, voicemail.FieldDuration:
			values[i] = new(sql.NullInt64)
		case voicemail.FieldCaller, voicemail.FieldAudioPath, voicemail.FieldTranscription:
			values[i] = new(sql.NullString)
		case voicemail.FieldCreatedAt, voicemail.FieldReadAt:
			values[i] = new(sql.NullTime)
		case voicemail.ForeignKeys[0]: // tenant_voicemails
			values[i] = new(sql.NullInt64)
		case voicemail.ForeignKeys[1]: // user_voicemails
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Voicemail fields.
func (_m *Voicemail) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case voicemail.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case voicemail.FieldCaller:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field caller", values[i])
			} else if value.Valid {
				_m.Caller = value.String
			}
		case voicemail.FieldAudioPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field audio_path", values[i])
			} else if value.Valid {
				_m.AudioPath = value.String
			}
		case voicemail.FieldTranscription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field transcription", values[i])
			} else if value.Valid {
				_m.Transcription = value.String
			}
		case voicemail.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case voicemail.FieldDuration:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field duration", values[i])
			} else if value.Valid {
				_m.Duration = int(value.Int64)
			}
		case voicemail.FieldReadAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field read_at", values[i])
			} else if value.Valid {
				_m.ReadAt = value.Time
			}
		case voicemail.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field tenant_voicemails", value)
			} else if value.Valid {
				_m.tenant_voicemails = new(int)
				*_m.tenant_voicemails = int(value.Int64)
			}
		case voicemail.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_voicemails", value)
			} else if value.Valid {
				_m.user_voicemails = new(int)
				*_m.user_voicemails = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Voicemail.
// This includes values selected through modifiers, order, etc.
func (_m *Voicemail) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the Voicemail entity.
func (_m *Voicemail) QueryTenant() *TenantQuery {
	return NewVoicemailClient(_m.config).QueryTenant(_m)
}

// QueryUser queries the "user" edge of the Voicemail entity.
func (_m *Voicemail) QueryUser() *UserQuery {
	return NewVoicemailClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this Voicemail.
// Note that you need to call Voicemail.Unwrap() before calling this method if this Voicemail
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Voicemail) Update() *VoicemailUpdateOne {
	return NewVoicemailClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Voicemail entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Voicemail) Unwrap() *Voicemail {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Voicemail is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Voicemail) String() string {
	var builder strings.Builder
	builder.WriteString("Voicemail(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("caller=")
	builder.WriteString(_m.Caller)
	builder.WriteString(", ")
	builder.WriteString("audio_path=")
	builder.WriteString(_m.AudioPath)
	builder.WriteString(", ")
	builder.WriteString("transcription=")
	builder.WriteString(_m.Transcription)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("duration=")
	builder.WriteString(fmt.Sprintf("%v", _m.Duration))
	builder.WriteString(", ")
	builder.WriteString("read_at=")
	builder.WriteString(_m.ReadAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Voicemails is a parsable slice of Voicemail.
type Voicemails []*Voicemail

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"sent/ent/networkbackup"
	"sent/ent/networkdevice"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// NetworkBackup is the model entity for the NetworkBackup schema.
type NetworkBackup struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ContentHash holds the value of the "content_hash" field.
	ContentHash string `json:"content_hash,omitempty"`
	// VaultPath holds the value of the "vault_path" field.
	VaultPath string `json:"vault_path,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the NetworkBackupQuery when eager-loading is set.
	Edges                  NetworkBackupEdges `json:"edges"`
	network_device_backups *int
	selectValues           sql.SelectValues
}

// NetworkBackupEdges holds the relations/edges for other nodes in the graph.
type NetworkBackupEdges struct {
	// Device holds the value of the device edge.
	Device *NetworkDevice `json:"device,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DeviceOrErr returns the Device value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e NetworkBackupEdges) DeviceOrErr() (*NetworkDevice, error) {
	if e.Device != nil {
		return e.Device, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: networkdevice.Label}
	}
	return nil, &NotLoadedError{edge: "device"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NetworkBackup) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case networkbackup.FieldID:
			values[i] = new(sql.NullInt64)
		case networkbackup.FieldContentHash, networkbackup.FieldVaultPath:
			values[i] = new(sql.NullString)
		case networkbackup.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case networkbackup.ForeignKeys[0]: // network_device_backups
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NetworkBackup fields.
func (_m *NetworkBackup) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case networkbackup.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case networkbackup.FieldContentHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content_hash", values[i])
			} else if value.Valid {
				_m.ContentHash = value.String
			}
		case networkbackup.FieldVaultPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field vault_path", values[i])
			} else if value.Valid {
				_m.VaultPath = value.String
			}
		case networkbackup.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case networkbackup.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field network_device_backups", value)
			} else if value.Valid {
				_m.network_device_backups = new(int)
				*_m.network_device_backups = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NetworkBackup.
// This includes values selected through modifiers, order, etc.
func (_m *NetworkBackup) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDevice queries the "device" edge of the NetworkBackup entity.
func (_m *NetworkBackup) QueryDevice() *NetworkDeviceQuery {
	return NewNetworkBackupClient(_m.config).QueryDevice(_m)
}

// Update returns a builder for updating this NetworkBackup.
// Note that you need to call NetworkBackup.Unwrap() before calling this method if this NetworkBackup
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *NetworkBackup) Update() *NetworkBackupUpdateOne {
	return NewNetworkBackupClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the NetworkBackup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *NetworkBackup) Unwrap() *NetworkBackup {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: NetworkBackup is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *NetworkBackup) String() string {
	var builder strings.Builder
	builder.WriteString("NetworkBackup(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("content_hash=")
	builder.WriteString(_m.ContentHash)
	builder.WriteString(", ")
	builder.WriteString("vault_path=")
	builder.WriteString(_m.VaultPath)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NetworkBackups is a parsable slice of NetworkBackup.
type NetworkBackups []*NetworkBackup

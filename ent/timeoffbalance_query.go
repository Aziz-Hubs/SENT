// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"sent/ent/employee"
	"sent/ent/predicate"
	"sent/ent/tenant"
	"sent/ent/timeoffbalance"
	"sent/ent/timeoffpolicy"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TimeOffBalanceQuery is the builder for querying TimeOffBalance entities.
type TimeOffBalanceQuery struct {
	config
	ctx          *QueryContext
	order        []timeoffbalance.OrderOption
	inters       []Interceptor
	predicates   []predicate.TimeOffBalance
	withTenant   *TenantQuery
	withEmployee *EmployeeQuery
	withPolicy   *TimeOffPolicyQuery
	withFKs      bool
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TimeOffBalanceQuery builder.
func (_q *TimeOffBalanceQuery) Where(ps ...predicate.TimeOffBalance) *TimeOffBalanceQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *TimeOffBalanceQuery) Limit(limit int) *TimeOffBalanceQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *TimeOffBalanceQuery) Offset(offset int) *TimeOffBalanceQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *TimeOffBalanceQuery) Unique(unique bool) *TimeOffBalanceQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *TimeOffBalanceQuery) Order(o ...timeoffbalance.OrderOption) *TimeOffBalanceQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryTenant chains the current query on the "tenant" edge.
func (_q *TimeOffBalanceQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(timeoffbalance.Table, timeoffbalance.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, timeoffbalance.TenantTable, timeoffbalance.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEmployee chains the current query on the "employee" edge.
func (_q *TimeOffBalanceQuery) QueryEmployee() *EmployeeQuery {
	query := (&EmployeeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(timeoffbalance.Table, timeoffbalance.FieldID, selector),
			sqlgraph.To(employee.Table, employee.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, timeoffbalance.EmployeeTable, timeoffbalance.EmployeeColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPolicy chains the current query on the "policy" edge.
func (_q *TimeOffBalanceQuery) QueryPolicy() *TimeOffPolicyQuery {
	query := (&TimeOffPolicyClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(timeoffbalance.Table, timeoffbalance.FieldID, selector),
			sqlgraph.To(timeoffpolicy.Table, timeoffpolicy.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, timeoffbalance.PolicyTable, timeoffbalance.PolicyColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TimeOffBalance entity from the query.
// Returns a *NotFoundError when no TimeOffBalance was found.
func (_q *TimeOffBalanceQuery) First(ctx context.Context) (*TimeOffBalance, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{timeoffbalance.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *TimeOffBalanceQuery) FirstX(ctx context.Context) *TimeOffBalance {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TimeOffBalance ID from the query.
// Returns a *NotFoundError when no TimeOffBalance ID was found.
func (_q *TimeOffBalanceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{timeoffbalance.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *TimeOffBalanceQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TimeOffBalance entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TimeOffBalance entity is found.
// Returns a *NotFoundError when no TimeOffBalance entities are found.
func (_q *TimeOffBalanceQuery) Only(ctx context.Context) (*TimeOffBalance, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{timeoffbalance.Label}
	default:
		return nil, &NotSingularError{timeoffbalance.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *TimeOffBalanceQuery) OnlyX(ctx context.Context) *TimeOffBalance {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TimeOffBalance ID in the query.
// Returns a *NotSingularError when more than one TimeOffBalance ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *TimeOffBalanceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{timeoffbalance.Label}
	default:
		err = &NotSingularError{timeoffbalance.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *TimeOffBalanceQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TimeOffBalances.
func (_q *TimeOffBalanceQuery) All(ctx context.Context) ([]*TimeOffBalance, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TimeOffBalance, *TimeOffBalanceQuery]()
	return withInterceptors[[]*TimeOffBalance](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *TimeOffBalanceQuery) AllX(ctx context.Context) []*TimeOffBalance {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TimeOffBalance IDs.
func (_q *TimeOffBalanceQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(timeoffbalance.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *TimeOffBalanceQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *TimeOffBalanceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*TimeOffBalanceQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *TimeOffBalanceQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *TimeOffBalanceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *TimeOffBalanceQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TimeOffBalanceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *TimeOffBalanceQuery) Clone() *TimeOffBalanceQuery {
	if _q == nil {
		return nil
	}
	return &TimeOffBalanceQuery{
		config:       _q.config,
		ctx:          _q.ctx.Clone(),
		order:        append([]timeoffbalance.OrderOption{}, _q.order...),
		inters:       append([]Interceptor{}, _q.inters...),
		predicates:   append([]predicate.TimeOffBalance{}, _q.predicates...),
		withTenant:   _q.withTenant.Clone(),
		withEmployee: _q.withEmployee.Clone(),
		withPolicy:   _q.withPolicy.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TimeOffBalanceQuery) WithTenant(opts ...func(*TenantQuery)) *TimeOffBalanceQuery {
	query := (&TenantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTenant = query
	return _q
}

// WithEmployee tells the query-builder to eager-load the nodes that are connected to
// the "employee" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TimeOffBalanceQuery) WithEmployee(opts ...func(*EmployeeQuery)) *TimeOffBalanceQuery {
	query := (&EmployeeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEmployee = query
	return _q
}

// WithPolicy tells the query-builder to eager-load the nodes that are connected to
// the "policy" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TimeOffBalanceQuery) WithPolicy(opts ...func(*TimeOffPolicyQuery)) *TimeOffBalanceQuery {
	query := (&TimeOffPolicyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPolicy = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Year int `json:"year,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TimeOffBalance.Query().
//		GroupBy(timeoffbalance.FieldYear).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *TimeOffBalanceQuery) GroupBy(field string, fields ...string) *TimeOffBalanceGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TimeOffBalanceGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = timeoffbalance.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Year int `json:"year,omitempty"`
//	}
//
//	client.TimeOffBalance.Query().
//		Select(timeoffbalance.FieldYear).
//		Scan(ctx, &v)
func (_q *TimeOffBalanceQuery) Select(fields ...string) *TimeOffBalanceSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &TimeOffBalanceSelect{TimeOffBalanceQuery: _q}
	sbuild.label = timeoffbalance.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TimeOffBalanceSelect configured with the given aggregations.
func (_q *TimeOffBalanceQuery) Aggregate(fns ...AggregateFunc) *TimeOffBalanceSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *TimeOffBalanceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !timeoffbalance.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *TimeOffBalanceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TimeOffBalance, error) {
	var (
		nodes       = []*TimeOffBalance{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withTenant != nil,
			_q.withEmployee != nil,
			_q.withPolicy != nil,
		}
	)
	if _q.withTenant != nil || _q.withEmployee != nil || _q.withPolicy != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, timeoffbalance.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TimeOffBalance).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TimeOffBalance{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withTenant; query != nil {
		if err := _q.loadTenant(ctx, query, nodes, nil,
			func(n *TimeOffBalance, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEmployee; query != nil {
		if err := _q.loadEmployee(ctx, query, nodes, nil,
			func(n *TimeOffBalance, e *Employee) { n.Edges.Employee = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPolicy; query != nil {
		if err := _q.loadPolicy(ctx, query, nodes, nil,
			func(n *TimeOffBalance, e *TimeOffPolicy) { n.Edges.Policy = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *TimeOffBalanceQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*TimeOffBalance, init func(*TimeOffBalance), assign func(*TimeOffBalance, *Tenant)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TimeOffBalance)
	for i := range nodes {
		if nodes[i].tenant_time_off_balances == nil {
			continue
		}
		fk := *nodes[i].tenant_time_off_balances
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_time_off_balances" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *TimeOffBalanceQuery) loadEmployee(ctx context.Context, query *EmployeeQuery, nodes []*TimeOffBalance, init func(*TimeOffBalance), assign func(*TimeOffBalance, *Employee)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TimeOffBalance)
	for i := range nodes {
		if nodes[i].employee_time_off_balances == nil {
			continue
		}
		fk := *nodes[i].employee_time_off_balances
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(employee.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "employee_time_off_balances" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *TimeOffBalanceQuery) loadPolicy(ctx context.Context, query *TimeOffPolicyQuery, nodes []*TimeOffBalance, init func(*TimeOffBalance), assign func(*TimeOffBalance, *TimeOffPolicy)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TimeOffBalance)
	for i := range nodes {
		if nodes[i].time_off_policy_balances == nil {
			continue
		}
		fk := *nodes[i].time_off_policy_balances
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(timeoffpolicy.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "time_off_policy_balances" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *TimeOffBalanceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *TimeOffBalanceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(timeoffbalance.Table, timeoffbalance.Columns, sqlgraph.NewFieldSpec(timeoffbalance.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, timeoffbalance.FieldID)
		for i := range fields {
			if fields[i] != timeoffbalance.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *TimeOffBalanceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(timeoffbalance.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = timeoffbalance.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *TimeOffBalanceQuery) Modify(modifiers ...func(s *sql.Selector)) *TimeOffBalanceSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// TimeOffBalanceGroupBy is the group-by builder for TimeOffBalance entities.
type TimeOffBalanceGroupBy struct {
	selector
	build *TimeOffBalanceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *TimeOffBalanceGroupBy) Aggregate(fns ...AggregateFunc) *TimeOffBalanceGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *TimeOffBalanceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TimeOffBalanceQuery, *TimeOffBalanceGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *TimeOffBalanceGroupBy) sqlScan(ctx context.Context, root *TimeOffBalanceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TimeOffBalanceSelect is the builder for selecting fields of TimeOffBalance entities.
type TimeOffBalanceSelect struct {
	*TimeOffBalanceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *TimeOffBalanceSelect) Aggregate(fns ...AggregateFunc) *TimeOffBalanceSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *TimeOffBalanceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TimeOffBalanceQuery, *TimeOffBalanceSelect](ctx, _s.TimeOffBalanceQuery, _s, _s.inters, v)
}

func (_s *TimeOffBalanceSelect) sqlScan(ctx context.Context, root *TimeOffBalanceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *TimeOffBalanceSelect) Modify(modifiers ...func(s *sql.Selector)) *TimeOffBalanceSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"sent/ent/agent"
	"sent/ent/job"
	"sent/ent/jobexecution"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// JobExecution is the model entity for the JobExecution schema.
type JobExecution struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Status holds the value of the "status" field.
	Status jobexecution.Status `json:"status,omitempty"`
	// Output holds the value of the "output" field.
	Output string `json:"output,omitempty"`
	// StartedAt holds the value of the "started_at" field.
	StartedAt time.Time `json:"started_at,omitempty"`
	// CompletedAt holds the value of the "completed_at" field.
	CompletedAt time.Time `json:"completed_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the JobExecutionQuery when eager-loading is set.
	Edges                JobExecutionEdges `json:"edges"`
	agent_job_executions *int
	job_executions       *int
	selectValues         sql.SelectValues
}

// JobExecutionEdges holds the relations/edges for other nodes in the graph.
type JobExecutionEdges struct {
	// Job holds the value of the job edge.
	Job *Job `json:"job,omitempty"`
	// Agent holds the value of the agent edge.
	Agent *Agent `json:"agent,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// JobOrErr returns the Job value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JobExecutionEdges) JobOrErr() (*Job, error) {
	if e.Job != nil {
		return e.Job, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: job.Label}
	}
	return nil, &NotLoadedError{edge: "job"}
}

// AgentOrErr returns the Agent value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JobExecutionEdges) AgentOrErr() (*Agent, error) {
	if e.Agent != nil {
		return e.Agent, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: agent.Label}
	}
	return nil, &NotLoadedError{edge: "agent"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JobExecution) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case jobexecution.FieldID:
			values[i] = new(sql.NullInt64)
		case jobexecution.FieldStatus, jobexecution.FieldOutput:
			values[i] = new(sql.NullString)
		case jobexecution.FieldStartedAt, jobexecution.FieldCompletedAt, jobexecution.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case jobexecution.ForeignKeys[0]: // agent_job_executions
			values[i] = new(sql.NullInt64)
		case jobexecution.ForeignKeys[1]: // job_executions
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JobExecution fields.
func (_m *JobExecution) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case jobexecution.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case jobexecution.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = jobexecution.Status(value.String)
			}
		case jobexecution.FieldOutput:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field output", values[i])
			} else if value.Valid {
				_m.Output = value.String
			}
		case jobexecution.FieldStartedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field started_at", values[i])
			} else if value.Valid {
				_m.StartedAt = value.Time
			}
		case jobexecution.FieldCompletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field completed_at", values[i])
			} else if value.Valid {
				_m.CompletedAt = value.Time
			}
		case jobexecution.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case jobexecution.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field agent_job_executions", value)
			} else if value.Valid {
				_m.agent_job_executions = new(int)
				*_m.agent_job_executions = int(value.Int64)
			}
		case jobexecution.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field job_executions", value)
			} else if value.Valid {
				_m.job_executions = new(int)
				*_m.job_executions = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JobExecution.
// This includes values selected through modifiers, order, etc.
func (_m *JobExecution) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryJob queries the "job" edge of the JobExecution entity.
func (_m *JobExecution) QueryJob() *JobQuery {
	return NewJobExecutionClient(_m.config).QueryJob(_m)
}

// QueryAgent queries the "agent" edge of the JobExecution entity.
func (_m *JobExecution) QueryAgent() *AgentQuery {
	return NewJobExecutionClient(_m.config).QueryAgent(_m)
}

// Update returns a builder for updating this JobExecution.
// Note that you need to call JobExecution.Unwrap() before calling this method if this JobExecution
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *JobExecution) Update() *JobExecutionUpdateOne {
	return NewJobExecutionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the JobExecution entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *JobExecution) Unwrap() *JobExecution {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: JobExecution is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *JobExecution) String() string {
	var builder strings.Builder
	builder.WriteString("JobExecution(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("output=")
	builder.WriteString(_m.Output)
	builder.WriteString(", ")
	builder.WriteString("started_at=")
	builder.WriteString(_m.StartedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("completed_at=")
	builder.WriteString(_m.CompletedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// JobExecutions is a parsable slice of JobExecution.
type JobExecutions []*JobExecution

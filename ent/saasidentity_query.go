// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"sent/ent/predicate"
	"sent/ent/saasapp"
	"sent/ent/saasidentity"
	"sent/ent/saasusage"
	"sent/ent/tenant"
	"sent/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SaaSIdentityQuery is the builder for querying SaaSIdentity entities.
type SaaSIdentityQuery struct {
	config
	ctx        *QueryContext
	order      []saasidentity.OrderOption
	inters     []Interceptor
	predicates []predicate.SaaSIdentity
	withUser   *UserQuery
	withApp    *SaaSAppQuery
	withUsages *SaaSUsageQuery
	withTenant *TenantQuery
	withFKs    bool
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SaaSIdentityQuery builder.
func (_q *SaaSIdentityQuery) Where(ps ...predicate.SaaSIdentity) *SaaSIdentityQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SaaSIdentityQuery) Limit(limit int) *SaaSIdentityQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SaaSIdentityQuery) Offset(offset int) *SaaSIdentityQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SaaSIdentityQuery) Unique(unique bool) *SaaSIdentityQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SaaSIdentityQuery) Order(o ...saasidentity.OrderOption) *SaaSIdentityQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryUser chains the current query on the "user" edge.
func (_q *SaaSIdentityQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(saasidentity.Table, saasidentity.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, saasidentity.UserTable, saasidentity.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryApp chains the current query on the "app" edge.
func (_q *SaaSIdentityQuery) QueryApp() *SaaSAppQuery {
	query := (&SaaSAppClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(saasidentity.Table, saasidentity.FieldID, selector),
			sqlgraph.To(saasapp.Table, saasapp.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, saasidentity.AppTable, saasidentity.AppColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUsages chains the current query on the "usages" edge.
func (_q *SaaSIdentityQuery) QueryUsages() *SaaSUsageQuery {
	query := (&SaaSUsageClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(saasidentity.Table, saasidentity.FieldID, selector),
			sqlgraph.To(saasusage.Table, saasusage.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, saasidentity.UsagesTable, saasidentity.UsagesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTenant chains the current query on the "tenant" edge.
func (_q *SaaSIdentityQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(saasidentity.Table, saasidentity.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, saasidentity.TenantTable, saasidentity.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SaaSIdentity entity from the query.
// Returns a *NotFoundError when no SaaSIdentity was found.
func (_q *SaaSIdentityQuery) First(ctx context.Context) (*SaaSIdentity, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{saasidentity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SaaSIdentityQuery) FirstX(ctx context.Context) *SaaSIdentity {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SaaSIdentity ID from the query.
// Returns a *NotFoundError when no SaaSIdentity ID was found.
func (_q *SaaSIdentityQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{saasidentity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SaaSIdentityQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SaaSIdentity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SaaSIdentity entity is found.
// Returns a *NotFoundError when no SaaSIdentity entities are found.
func (_q *SaaSIdentityQuery) Only(ctx context.Context) (*SaaSIdentity, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{saasidentity.Label}
	default:
		return nil, &NotSingularError{saasidentity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SaaSIdentityQuery) OnlyX(ctx context.Context) *SaaSIdentity {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SaaSIdentity ID in the query.
// Returns a *NotSingularError when more than one SaaSIdentity ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SaaSIdentityQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{saasidentity.Label}
	default:
		err = &NotSingularError{saasidentity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SaaSIdentityQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SaaSIdentities.
func (_q *SaaSIdentityQuery) All(ctx context.Context) ([]*SaaSIdentity, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SaaSIdentity, *SaaSIdentityQuery]()
	return withInterceptors[[]*SaaSIdentity](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SaaSIdentityQuery) AllX(ctx context.Context) []*SaaSIdentity {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SaaSIdentity IDs.
func (_q *SaaSIdentityQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(saasidentity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SaaSIdentityQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SaaSIdentityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SaaSIdentityQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SaaSIdentityQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SaaSIdentityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SaaSIdentityQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SaaSIdentityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SaaSIdentityQuery) Clone() *SaaSIdentityQuery {
	if _q == nil {
		return nil
	}
	return &SaaSIdentityQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]saasidentity.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.SaaSIdentity{}, _q.predicates...),
		withUser:   _q.withUser.Clone(),
		withApp:    _q.withApp.Clone(),
		withUsages: _q.withUsages.Clone(),
		withTenant: _q.withTenant.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SaaSIdentityQuery) WithUser(opts ...func(*UserQuery)) *SaaSIdentityQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithApp tells the query-builder to eager-load the nodes that are connected to
// the "app" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SaaSIdentityQuery) WithApp(opts ...func(*SaaSAppQuery)) *SaaSIdentityQuery {
	query := (&SaaSAppClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withApp = query
	return _q
}

// WithUsages tells the query-builder to eager-load the nodes that are connected to
// the "usages" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SaaSIdentityQuery) WithUsages(opts ...func(*SaaSUsageQuery)) *SaaSIdentityQuery {
	query := (&SaaSUsageClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUsages = query
	return _q
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SaaSIdentityQuery) WithTenant(opts ...func(*TenantQuery)) *SaaSIdentityQuery {
	query := (&TenantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTenant = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ExternalID string `json:"external_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SaaSIdentity.Query().
//		GroupBy(saasidentity.FieldExternalID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SaaSIdentityQuery) GroupBy(field string, fields ...string) *SaaSIdentityGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SaaSIdentityGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = saasidentity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ExternalID string `json:"external_id,omitempty"`
//	}
//
//	client.SaaSIdentity.Query().
//		Select(saasidentity.FieldExternalID).
//		Scan(ctx, &v)
func (_q *SaaSIdentityQuery) Select(fields ...string) *SaaSIdentitySelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SaaSIdentitySelect{SaaSIdentityQuery: _q}
	sbuild.label = saasidentity.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SaaSIdentitySelect configured with the given aggregations.
func (_q *SaaSIdentityQuery) Aggregate(fns ...AggregateFunc) *SaaSIdentitySelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SaaSIdentityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !saasidentity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SaaSIdentityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SaaSIdentity, error) {
	var (
		nodes       = []*SaaSIdentity{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withUser != nil,
			_q.withApp != nil,
			_q.withUsages != nil,
			_q.withTenant != nil,
		}
	)
	if _q.withUser != nil || _q.withApp != nil || _q.withTenant != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, saasidentity.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SaaSIdentity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SaaSIdentity{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes, nil,
			func(n *SaaSIdentity, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withApp; query != nil {
		if err := _q.loadApp(ctx, query, nodes, nil,
			func(n *SaaSIdentity, e *SaaSApp) { n.Edges.App = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUsages; query != nil {
		if err := _q.loadUsages(ctx, query, nodes,
			func(n *SaaSIdentity) { n.Edges.Usages = []*SaaSUsage{} },
			func(n *SaaSIdentity, e *SaaSUsage) { n.Edges.Usages = append(n.Edges.Usages, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTenant; query != nil {
		if err := _q.loadTenant(ctx, query, nodes, nil,
			func(n *SaaSIdentity, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SaaSIdentityQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*SaaSIdentity, init func(*SaaSIdentity), assign func(*SaaSIdentity, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SaaSIdentity)
	for i := range nodes {
		if nodes[i].user_saas_identities == nil {
			continue
		}
		fk := *nodes[i].user_saas_identities
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_saas_identities" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SaaSIdentityQuery) loadApp(ctx context.Context, query *SaaSAppQuery, nodes []*SaaSIdentity, init func(*SaaSIdentity), assign func(*SaaSIdentity, *SaaSApp)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SaaSIdentity)
	for i := range nodes {
		if nodes[i].saa_sapp_identities == nil {
			continue
		}
		fk := *nodes[i].saa_sapp_identities
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(saasapp.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "saa_sapp_identities" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SaaSIdentityQuery) loadUsages(ctx context.Context, query *SaaSUsageQuery, nodes []*SaaSIdentity, init func(*SaaSIdentity), assign func(*SaaSIdentity, *SaaSUsage)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*SaaSIdentity)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.SaaSUsage(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(saasidentity.UsagesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.saa_sidentity_usages
		if fk == nil {
			return fmt.Errorf(`foreign-key "saa_sidentity_usages" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "saa_sidentity_usages" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SaaSIdentityQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*SaaSIdentity, init func(*SaaSIdentity), assign func(*SaaSIdentity, *Tenant)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*SaaSIdentity)
	for i := range nodes {
		if nodes[i].tenant_saas_identities == nil {
			continue
		}
		fk := *nodes[i].tenant_saas_identities
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_saas_identities" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *SaaSIdentityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SaaSIdentityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(saasidentity.Table, saasidentity.Columns, sqlgraph.NewFieldSpec(saasidentity.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, saasidentity.FieldID)
		for i := range fields {
			if fields[i] != saasidentity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SaaSIdentityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(saasidentity.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = saasidentity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *SaaSIdentityQuery) Modify(modifiers ...func(s *sql.Selector)) *SaaSIdentitySelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// SaaSIdentityGroupBy is the group-by builder for SaaSIdentity entities.
type SaaSIdentityGroupBy struct {
	selector
	build *SaaSIdentityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SaaSIdentityGroupBy) Aggregate(fns ...AggregateFunc) *SaaSIdentityGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SaaSIdentityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SaaSIdentityQuery, *SaaSIdentityGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SaaSIdentityGroupBy) sqlScan(ctx context.Context, root *SaaSIdentityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SaaSIdentitySelect is the builder for selecting fields of SaaSIdentity entities.
type SaaSIdentitySelect struct {
	*SaaSIdentityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SaaSIdentitySelect) Aggregate(fns ...AggregateFunc) *SaaSIdentitySelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SaaSIdentitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SaaSIdentityQuery, *SaaSIdentitySelect](ctx, _s.SaaSIdentityQuery, _s, _s.inters, v)
}

func (_s *SaaSIdentitySelect) sqlScan(ctx context.Context, root *SaaSIdentityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *SaaSIdentitySelect) Modify(modifiers ...func(s *sql.Selector)) *SaaSIdentitySelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}

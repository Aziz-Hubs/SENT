// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"sent/ent/compensationagreement"
	"sent/ent/employee"
	"sent/ent/tenant"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CompensationAgreement is the model entity for the CompensationAgreement schema.
type CompensationAgreement struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// BaseSalary holds the value of the "base_salary" field.
	BaseSalary float64 `json:"base_salary,omitempty"`
	// Currency holds the value of the "currency" field.
	Currency string `json:"currency,omitempty"`
	// EffectiveDate holds the value of the "effective_date" field.
	EffectiveDate time.Time `json:"effective_date,omitempty"`
	// Status holds the value of the "status" field.
	Status compensationagreement.Status `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CompensationAgreementQuery when eager-loading is set.
	Edges                            CompensationAgreementEdges `json:"edges"`
	employee_compensation_agreements *int
	tenant_compensation_agreements   *int
	selectValues                     sql.SelectValues
}

// CompensationAgreementEdges holds the relations/edges for other nodes in the graph.
type CompensationAgreementEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Employee holds the value of the employee edge.
	Employee *Employee `json:"employee,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CompensationAgreementEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// EmployeeOrErr returns the Employee value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CompensationAgreementEdges) EmployeeOrErr() (*Employee, error) {
	if e.Employee != nil {
		return e.Employee, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: employee.Label}
	}
	return nil, &NotLoadedError{edge: "employee"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CompensationAgreement) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case compensationagreement.FieldBaseSalary:
			values[i] = new(sql.NullFloat64)
		case compensationagreement.FieldID:
			values[i] = new(sql.NullInt64)
		case compensationagreement.FieldCurrency, compensationagreement.FieldStatus:
			values[i] = new(sql.NullString)
		case compensationagreement.FieldEffectiveDate, compensationagreement.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case compensationagreement.ForeignKeys[0]: // employee_compensation_agreements
			values[i] = new(sql.NullInt64)
		case compensationagreement.ForeignKeys[1]: // tenant_compensation_agreements
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CompensationAgreement fields.
func (_m *CompensationAgreement) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case compensationagreement.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case compensationagreement.FieldBaseSalary:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field base_salary", values[i])
			} else if value.Valid {
				_m.BaseSalary = value.Float64
			}
		case compensationagreement.FieldCurrency:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field currency", values[i])
			} else if value.Valid {
				_m.Currency = value.String
			}
		case compensationagreement.FieldEffectiveDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field effective_date", values[i])
			} else if value.Valid {
				_m.EffectiveDate = value.Time
			}
		case compensationagreement.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = compensationagreement.Status(value.String)
			}
		case compensationagreement.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case compensationagreement.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field employee_compensation_agreements", value)
			} else if value.Valid {
				_m.employee_compensation_agreements = new(int)
				*_m.employee_compensation_agreements = int(value.Int64)
			}
		case compensationagreement.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field tenant_compensation_agreements", value)
			} else if value.Valid {
				_m.tenant_compensation_agreements = new(int)
				*_m.tenant_compensation_agreements = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CompensationAgreement.
// This includes values selected through modifiers, order, etc.
func (_m *CompensationAgreement) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the CompensationAgreement entity.
func (_m *CompensationAgreement) QueryTenant() *TenantQuery {
	return NewCompensationAgreementClient(_m.config).QueryTenant(_m)
}

// QueryEmployee queries the "employee" edge of the CompensationAgreement entity.
func (_m *CompensationAgreement) QueryEmployee() *EmployeeQuery {
	return NewCompensationAgreementClient(_m.config).QueryEmployee(_m)
}

// Update returns a builder for updating this CompensationAgreement.
// Note that you need to call CompensationAgreement.Unwrap() before calling this method if this CompensationAgreement
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CompensationAgreement) Update() *CompensationAgreementUpdateOne {
	return NewCompensationAgreementClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CompensationAgreement entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CompensationAgreement) Unwrap() *CompensationAgreement {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CompensationAgreement is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CompensationAgreement) String() string {
	var builder strings.Builder
	builder.WriteString("CompensationAgreement(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("base_salary=")
	builder.WriteString(fmt.Sprintf("%v", _m.BaseSalary))
	builder.WriteString(", ")
	builder.WriteString("currency=")
	builder.WriteString(_m.Currency)
	builder.WriteString(", ")
	builder.WriteString("effective_date=")
	builder.WriteString(_m.EffectiveDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CompensationAgreements is a parsable slice of CompensationAgreement.
type CompensationAgreements []*CompensationAgreement

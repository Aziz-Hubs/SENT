// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"sent/ent/product"
	"sent/ent/stockmovement"
	"sent/ent/tenant"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// StockMovement is the model entity for the StockMovement schema.
type StockMovement struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity float64 `json:"quantity,omitempty"`
	// MovementType holds the value of the "movement_type" field.
	MovementType stockmovement.MovementType `json:"movement_type,omitempty"`
	// Reason holds the value of the "reason" field.
	Reason string `json:"reason,omitempty"`
	// UnitCost holds the value of the "unit_cost" field.
	UnitCost float64 `json:"unit_cost,omitempty"`
	// RemainingQuantity holds the value of the "remaining_quantity" field.
	RemainingQuantity *float64 `json:"remaining_quantity,omitempty"`
	// CalculatedCogs holds the value of the "calculated_cogs" field.
	CalculatedCogs *float64 `json:"calculated_cogs,omitempty"`
	// Metadata holds the value of the "metadata" field.
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the StockMovementQuery when eager-loading is set.
	Edges                  StockMovementEdges `json:"edges"`
	product_movements      *int
	tenant_stock_movements *int
	selectValues           sql.SelectValues
}

// StockMovementEdges holds the relations/edges for other nodes in the graph.
type StockMovementEdges struct {
	// Product holds the value of the product edge.
	Product *Product `json:"product,omitempty"`
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ProductOrErr returns the Product value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StockMovementEdges) ProductOrErr() (*Product, error) {
	if e.Product != nil {
		return e.Product, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: product.Label}
	}
	return nil, &NotLoadedError{edge: "product"}
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StockMovementEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StockMovement) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case stockmovement.FieldMetadata:
			values[i] = new([]byte)
		case stockmovement.FieldQuantity, stockmovement.FieldUnitCost, stockmovement.FieldRemainingQuantity, stockmovement.FieldCalculatedCogs:
			values[i] = new(sql.NullFloat64)
		case stockmovement.FieldID:
			values[i] = new(sql.NullInt64)
		case stockmovement.FieldMovementType, stockmovement.FieldReason:
			values[i] = new(sql.NullString)
		case stockmovement.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case stockmovement.ForeignKeys[0]: // product_movements
			values[i] = new(sql.NullInt64)
		case stockmovement.ForeignKeys[1]: // tenant_stock_movements
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StockMovement fields.
func (_m *StockMovement) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case stockmovement.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case stockmovement.FieldQuantity:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				_m.Quantity = value.Float64
			}
		case stockmovement.FieldMovementType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field movement_type", values[i])
			} else if value.Valid {
				_m.MovementType = stockmovement.MovementType(value.String)
			}
		case stockmovement.FieldReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field reason", values[i])
			} else if value.Valid {
				_m.Reason = value.String
			}
		case stockmovement.FieldUnitCost:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field unit_cost", values[i])
			} else if value.Valid {
				_m.UnitCost = value.Float64
			}
		case stockmovement.FieldRemainingQuantity:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field remaining_quantity", values[i])
			} else if value.Valid {
				_m.RemainingQuantity = new(float64)
				*_m.RemainingQuantity = value.Float64
			}
		case stockmovement.FieldCalculatedCogs:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field calculated_cogs", values[i])
			} else if value.Valid {
				_m.CalculatedCogs = new(float64)
				*_m.CalculatedCogs = value.Float64
			}
		case stockmovement.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		case stockmovement.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case stockmovement.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field product_movements", value)
			} else if value.Valid {
				_m.product_movements = new(int)
				*_m.product_movements = int(value.Int64)
			}
		case stockmovement.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field tenant_stock_movements", value)
			} else if value.Valid {
				_m.tenant_stock_movements = new(int)
				*_m.tenant_stock_movements = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StockMovement.
// This includes values selected through modifiers, order, etc.
func (_m *StockMovement) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryProduct queries the "product" edge of the StockMovement entity.
func (_m *StockMovement) QueryProduct() *ProductQuery {
	return NewStockMovementClient(_m.config).QueryProduct(_m)
}

// QueryTenant queries the "tenant" edge of the StockMovement entity.
func (_m *StockMovement) QueryTenant() *TenantQuery {
	return NewStockMovementClient(_m.config).QueryTenant(_m)
}

// Update returns a builder for updating this StockMovement.
// Note that you need to call StockMovement.Unwrap() before calling this method if this StockMovement
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *StockMovement) Update() *StockMovementUpdateOne {
	return NewStockMovementClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the StockMovement entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *StockMovement) Unwrap() *StockMovement {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: StockMovement is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *StockMovement) String() string {
	var builder strings.Builder
	builder.WriteString("StockMovement(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Quantity))
	builder.WriteString(", ")
	builder.WriteString("movement_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.MovementType))
	builder.WriteString(", ")
	builder.WriteString("reason=")
	builder.WriteString(_m.Reason)
	builder.WriteString(", ")
	builder.WriteString("unit_cost=")
	builder.WriteString(fmt.Sprintf("%v", _m.UnitCost))
	builder.WriteString(", ")
	if v := _m.RemainingQuantity; v != nil {
		builder.WriteString("remaining_quantity=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.CalculatedCogs; v != nil {
		builder.WriteString("calculated_cogs=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", _m.Metadata))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// StockMovements is a parsable slice of StockMovement.
type StockMovements []*StockMovement

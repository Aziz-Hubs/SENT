// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: create_agent.sql

package pulsedb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createAgent = `-- name: CreateAgent :one
INSERT INTO
    agents (
        tenant_id,
        hostname,
        os,
        arch,
        ip,
        mac,
        version,
        status
    )
VALUES (
        $1,
        $2,
        $3,
        $4,
        $5,
        $6,
        $7,
        $8
    )
RETURNING
    id, tenant_id, hostname, os, arch, ip, mac, version, status, last_seen, created_at, updated_at
`

type CreateAgentParams struct {
	TenantID pgtype.Int4 `json:"tenant_id"`
	Hostname string      `json:"hostname"`
	Os       pgtype.Text `json:"os"`
	Arch     pgtype.Text `json:"arch"`
	Ip       pgtype.Text `json:"ip"`
	Mac      pgtype.Text `json:"mac"`
	Version  pgtype.Text `json:"version"`
	Status   pgtype.Text `json:"status"`
}

func (q *Queries) CreateAgent(ctx context.Context, arg CreateAgentParams) (Agent, error) {
	row := q.db.QueryRow(ctx, createAgent,
		arg.TenantID,
		arg.Hostname,
		arg.Os,
		arg.Arch,
		arg.Ip,
		arg.Mac,
		arg.Version,
		arg.Status,
	)
	var i Agent
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Hostname,
		&i.Os,
		&i.Arch,
		&i.Ip,
		&i.Mac,
		&i.Version,
		&i.Status,
		&i.LastSeen,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
